# 📊 รายละเอียดโครงการ: ระบบเช็คอินพนักงาน (EMP_CHECKIN)

ตารางฐานข้อมูลนี้ถูกออกแบบมาเพื่อรองรับการลงทะเบียนและเช็คอินพนักงานจำนวน **2,000 คน** โดยเน้นความเร็วในการค้นหาและการตรวจสอบสถานะแบบ Real-time เพื่อประสิทธิภาพสูงสุดหน้างาน

---

## 🏗 โครงสร้างตาราง (Table Schema)

| ชื่อฟิลด์ (Column) | ประเภทข้อมูล (Type) | คำอธิบาย (Description) |
| :--- | :--- | :--- |
| **QR_CODE** | `VARCHAR2(100)` | รหัสสำหรับสร้าง QR Code (ปกติใช้รหัสเดียวกับ `EMP_ID`) |
| **EMP_ID** | `VARCHAR2(50)` | รหัสพนักงาน (ใช้เป็น **Primary Key** ในการตรวจสอบตัวตน) |
| **EMP_NAME** | `VARCHAR2(200)` | ชื่อ-นามสกุล ของพนักงาน |
| **PLANT** | `VARCHAR2(100)` | โรงงานที่สังกัด (เช่น Plant A, B, C) สำหรับแยกกลุ่มใน Dashboard |
| **STATUS** | `VARCHAR2(20)` | สถานะ: `PENDING` (ลงทะเบียนแล้ว) หรือ `DONE` (สแกนเข้างานแล้ว) |
| **SCAN_TIME** | `TIMESTAMP` | วันที่และเวลาที่พนักงานสแกนผ่านประตูเข้างานจริง |

---

## 🛠 กลยุทธ์การเพิ่มประสิทธิภาพ (Indexing)

เพื่อให้ระบบสแกนหน้างานไม่เกิดการคอขวด (Delay) ในช่วงเวลาที่มีพนักงานมาพร้อมกันจำนวนมาก เราได้สร้าง Index ไว้ 2 จุดสำคัญ:

1.  **`IDX_QR_SCAN`**: สร้างบนคอลัมน์ `QR_CODE` เพื่อให้ API สำหรับการเช็คอินสามารถ Query ข้อมูลได้รวดเร็วในระดับมิลลิวินาที
2.  **`IDX_EMP_ID`**: สร้างบนคอลัมน์ `EMP_ID` เพื่อใช้รองรับการตรวจสอบการลงทะเบียนซ้ำ และหน้าดึงข้อมูลบัตรเดิม (`view_old_ticket.php`)

---

## 💡 ตรรกะการทำงาน (Business Logic)

### 1. การจัดการสถานะ (Status Management)
* **Registration:** เมื่อบันทึกข้อมูลครั้งแรก ระบบจะกำหนดค่าเริ่มต้นเป็น `PENDING` เพื่อเตรียมรายชื่อสำหรับระบบ Lucky Draw
* **Check-in:** เมื่อมีการสแกน QR Code ระบบจะเปลี่ยนสถานะจาก `PENDING` เป็น `DONE` พร้อมประทับเวลาปัจจุบันลงใน `SCAN_TIME`

### 2. การป้องกันข้อมูลผิดพลาด (Data Integrity)
* **Duplicate Prevention:** ใช้ `EMP_ID` เป็นตัวคัดกรองหลักเพื่อป้องกันการลงทะเบียนซ้ำ
* **Idempotency:** ใช้เงื่อนไขการ Update เฉพาะแถวที่มีสถานะเป็น `PENDING` เท่านั้น เพื่อป้องกันปัญหาเวลาคลาดเคลื่อนในกรณีที่พนักงานสแกน QR Code ซ้ำหลายครั้ง

---

## 🔍 ตัวอย่างคำสั่ง SQL ที่ใช้บ่อย

### 1. สรุปยอดคนเข้างานแยกตาม Plant (สำหรับ Dashboard)
แสดงผลสรุปจำนวนพนักงานที่เข้างานแล้วตามกลุ่มโรงงาน

```sql
SELECT 
    PLANT, 
    COUNT(*) AS TOTAL_IN 
FROM 
    EMP_CHECKIN 
WHERE 
    STATUS = 'DONE' 
GROUP BY 
    PLANT;



